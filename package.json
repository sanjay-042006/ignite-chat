{
  "name": "ignitechat-deploy",
  "version": "1.0.0",
  "description": "Root package for deploying the full stack app on Railway, Render, or Heroku",
  "scripts": {
    "prebuild": "cd backend && npm install && npx prisma generate && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
